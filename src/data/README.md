Este directorio se reserva para fuentes de datos estáticos o definiciones de “fixtures”.

En esta prueba la persistencia se maneja principalmente mediante `localStorage` a través de `TaskRepository` y `AuthRepository`.


