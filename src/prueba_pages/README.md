Este directorio está reservado para páginas tradicionales de Next.js (`pages/`).

En este proyecto las rutas se implementan con **App Router** dentro de `app/`, pero se mantiene la carpeta para cumplir con la estructura sugerida en la prueba técnica.


